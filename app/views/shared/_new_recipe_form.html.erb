<%= form_for(@recipe) do |f| %> 
    <%= render 'shared/error_messages', object: f.object %> 
    <div class="field"> 
        <%= f.text_area :steps, placeholder: "Compose new recipe..." %> 
    </div> 
    <%= f.submit "Post", class: "btn btn-primary" %> 
<% end %>
